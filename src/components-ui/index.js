// NOTE: this file should only contain the component registrations

import CustomText from "./custom-text-react";
import CustomRoute from "./custom-route-react";

export default {
  CustomText,
  CustomRoute
  // export the CustomText react UI component
};
